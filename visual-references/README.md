# Visual references

Place the reference images for perceptual matching in this directory.

Supported image formats: `png`, `jpg`, `jpeg`, `gif`, `webp`, `bmp`, `tif`,
`tiff`, and `avif`.

Hashes are generated during the build step and written to
`generated/visual-reference-manifest.json`.
